export type {
  TouchPoint,
  TouchEvent,
  TouchCalibration,
  RawTouchPoint,
} from "@embedded/display-schema";

export {
  DEFAULT_TOUCH_CALIBRATION,
  TOUCH_MAX_X,
  TOUCH_MAX_Y,
} from "@embedded/display-schema";
